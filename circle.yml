deployment:
    staging:
        branch: staging
        commands:
            - ./scripts/deploy --staging
    production:
        branch: production
        commands:
            - ./scripts/deploy --production
